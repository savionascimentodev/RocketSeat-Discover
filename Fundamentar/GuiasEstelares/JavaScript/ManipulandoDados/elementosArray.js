// Manipulando Arrays

// Contar elementos de um array;

const array = ["a", 1, true, function () { return 'alo' }]

console.log(array.length);

// Cada tipo de dado pode ser diferente, incluindo functions;